#ifndef Fibonacci_h
#define Fibonacci_h

#include <cstdint>
#include <vector>
#include <math.h>

class Fibonacci
{
    public:

        static uint64_t calculateNthFibonacciNumber1(uint32_t n);

        static uint64_t calculateNthFibonacciNumber2(uint32_t n);

        static uint64_t calculateNthFibonacciNumber3(uint32_t n);

        static uint64_t calculateNthFibonacciNumber4(uint32_t n);

        static uint64_t calculateNthFibonacciNumber5(uint32_t n);

        static uint64_t calculateNthFibonacciNumber6(uint32_t n);
};

#endif // Fibonacci_h
